opensearch:
  instances:
    dev:
      url: "https://opensearch-instance1.example.com:9200"
      username: "admin1"
      password: ${OPENSEARCH_DEV_PASSWORD}
      sqlFetchSize: 1000
      insecureSkipVerify: false
    prod:
      url: "https://opensearch-instance2.example.com:9200"
      username: "admin2"
      password: ${OPENSEARCH_PROD_PASSWORD}
      insecureSkipVerify: true

llm:
  provider: "openai"
  model: "test-model"
  apiKey: ${LLM_API_KEY}
  serverURL: ""
  temperature: 0.7
