VENATOR_IMAGE=ghcr.io/0x4d31/venator:v0.2.0
VENATOR_CONFIG_DIR=../../config
VENATOR_UID=65532
VENATOR_GID=65532

# Add connector credentials in an untracked .env file or export them from a
# secret manager before invoking Docker Compose.
OPENSEARCH_PASSWORD=replace-me
SLACK_WEBHOOK_URL=replace-me
