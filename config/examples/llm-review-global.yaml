runtime:
  maxRecords: 1000
  maxBytes: 16777216
  timeout: 5m

ndjson:
  instances:
    review-events:
      path: ./events.ndjson

llm:
  provider: openai
  apiKey: ${OPENAI_API_KEY}
  model: ${OPENAI_MODEL}
  timeout: 30s
