name: llm-review-example
uid: 0501222a-3135-4264-b15b-62fd09ab6ff2
status: test
confidence: low
enabled: false
schedule: "0 0 * * *"
queryEngine: bigquery.dev
publishers:
  - pubsub.alerts
language: SQL
query: |
  SELECT actor, resource, rule_name, message, timestamp
  FROM `example-project.security.signals`
  WHERE timestamp >= TIMESTAMP_SUB(CURRENT_TIMESTAMP(), INTERVAL 24 HOUR)
output:
  format: raw
  fields: []
description: Demonstrates bounded advisory review of deterministic findings.
tags: [llm, example]
references: []
author: 0x4D31
ttps: []
llm:
  enabled: true
  required: false
  maxFindings: 25
  prompt: Assess whether each supplied security finding deserves analyst attention. Base the assessment only on the supplied evidence.
